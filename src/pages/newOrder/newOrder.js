define(["knockout", "text!./newOrder.html"], function(ko, template) {

    function NewOrderViewModel(route) {
        activate();

        function activate() {
        }
    }

    return { viewModel: NewOrderViewModel, template: template };
});
